<%- if Rails.env.development? %>
  <script src="/assets/bundle.js"></script>
  <script>
    $.ajaxSetup({
      headers: {
        'X-CSRF-Token': $('meta[name="csrf-token"]').attr('content')
      }
    });

    window.assets = {
      logoSVG: "<%= image_url("app/assets/images/logo/logo.svg") %>"
    };
  </script>
<%- end %>

<div id="root"></div>
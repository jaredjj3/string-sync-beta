import * as React from 'react';
import { compose } from 'recompose';

const enhance = compose (

);

const NotationShowMenu = () => (
  <div className="NotationShowMenu">
    NotationShowMenu
  </div>
);

export default enhance(NotationShowMenu);

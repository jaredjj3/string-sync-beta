import React from 'react';

interface FretsProps {}

interface FretsState {}

class Frets extends React.Component<FretsProps, FretsState> {
  render(): JSX.Element {
    return (
      <div className="Frets">
        Frets
      </div>
    );
  }
}

export default Frets;

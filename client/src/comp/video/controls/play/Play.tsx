import React from 'react';

interface PlayProps {}

interface PlayState {}

class Play extends React.Component<PlayProps, PlayState> {
  render(): JSX.Element {
    return (<span>Play</span>);
  }
}

export default Play;

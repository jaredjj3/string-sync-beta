import React from 'react';

interface VolumeProps {}

interface VolumeState {}

class Volume extends React.Component<VolumeProps, VolumeState> {
  render(): JSX.Element {
    return (<span>Volume</span>);
  }
}

export default Volume;
